// BT-1490: ExduraEvent — a single entry in a workflow's event history.
// See SPEC.md section 4.1.2.

Value subclass: ExduraEvent
  state: eventId :: Integer = 0
  state: eventType :: Symbol = #unknown
  state: timestamp :: Integer = 0
  state: payload :: Dictionary = #{}
